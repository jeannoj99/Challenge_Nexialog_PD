# Challenge_Nexialog_PD

Version Python requise: `3.11`

Créer un venv, l'activer puis installer les requirements:

```
pip install -r requirements.txt
```

Et enfin lancer le script:

```
python main.py
```